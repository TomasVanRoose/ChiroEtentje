
<h4>Voeg een nieuwe categorie toe</h1>

<%= bootstrap_form_for(@category)  do |f| %>
	<%= f.text_field :name, hide_label: true, placeholder: "bv.: voorgerecht, nagerecht, diversen..." %>
	<%= f.submit "Maak categorie aan" %>

<% end %>
